import { useEffect, useState } from 'react';
import { Alert, Text } from 'react-native';
import { router } from 'expo-router';
import { Screen } from '@/components/Screen';
import { AppButton, Card, Chips, IconButton, SectionTitle } from '@/components/ui';
import { getSettings, saveSettings } from '@/services/settings';
export default function Settings(){const [d,setD]=useState<any>(null);useEffect(()=>{void getSettings().then(setD)},[]);if(!d)return <Screen title="Cài đặt"><Card><Text>Đang tải…</Text></Card></Screen>;const patch=(p:any)=>setD({...d,...p});return <Screen title="Cài đặt" subtitle="Mẫu xuất • bố cục • ngôn ngữ" right={<IconButton icon="close" onPress={()=>router.back()}/>}><Card><SectionTitle>Ngôn ngữ giao diện</SectionTitle><Chips value={d.ui_language||'vi'} onChange={v=>patch({ui_language:v})} options={[{value:'vi',label:'Tiếng Việt'},{value:'en',label:'English'}]}/><SectionTitle>Ngôn ngữ chứng từ</SectionTitle><Chips value={d.document_language||'vi'} onChange={v=>patch({document_language:v})} options={[{value:'vi',label:'Tiếng Việt'},{value:'en',label:'English'}]}/></Card><Card><SectionTitle>Mẫu chứng từ</SectionTitle><Chips value={d.export_template||'classic'} onChange={v=>patch({export_template:v})} options={[{value:'classic',label:'Classic'},{value:'minimal',label:'Minimal'},{value:'professional',label:'Professional'},{value:'modern',label:'Modern'},{value:'corporate',label:'Corporate'},{value:'premium',label:'Premium'},{value:'service',label:'Service'},{value:'product',label:'Product/Image'},{value:'mobile-clean',label:'Mobile Clean'},{value:'mobile-pro',label:'Mobile Pro'}]}/><SectionTitle>Hướng giấy</SectionTitle><Chips value={d.export_orientation||'portrait'} onChange={v=>patch({export_orientation:v})} options={[{value:'portrait',label:'Dọc'},{value:'landscape',label:'Ngang'},{value:'auto',label:'Tự động'}]}/><SectionTitle>Mật độ dòng</SectionTitle><Chips value={d.export_layout_density||'normal'} onChange={v=>patch({export_layout_density:v})} options={[{value:'compact',label:'Gọn'},{value:'normal',label:'Chuẩn'},{value:'comfortable',label:'Thoáng'}]}/><SectionTitle>Kích thước ảnh</SectionTitle><Chips value={d.export_image_size||'medium'} onChange={v=>patch({export_image_size:v})} options={[{value:'small',label:'Nhỏ'},{value:'medium',label:'Vừa'},{value:'large',label:'Lớn'}]}/><SectionTitle>Fit ảnh</SectionTitle><Chips value={d.export_image_fit||'cover'} onChange={v=>patch({export_image_fit:v})} options={[{value:'cover',label:'Cover'},{value:'contain',label:'Contain'}]}/></Card><AppButton title="Lưu cài đặt" icon="content-save" onPress={()=>void saveSettings(d).then(()=>Alert.alert('Đã lưu','Cài đặt đã được áp dụng.'))}/></Screen>}
